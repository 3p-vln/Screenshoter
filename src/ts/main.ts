import {burgerMenu} from "./components/burger.ts";
import {dropDownMenu} from "./components/dropdown.ts";

document.addEventListener("DOMContentLoaded", ()=>{
    burgerMenu();
    dropDownMenu();
});

// addValToLink();